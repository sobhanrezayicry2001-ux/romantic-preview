body {
  margin: 0;
  padding: 0;
  font-family: 'Cursive', sans-serif;
  display: flex;
  justify-content: center;
  align-items: center;
  height: 100vh;
  background: linear-gradient(135deg, #ffb6c1, #dda0dd); /* صورتی به بنفش */
  color: #fff;
}

#container {
  text-align: center;
}

#startBtn {
  font-size: 28px;
  padding: 15px 30px;
  background-color: rgba(255,255,255,0.3);
  border: 2px solid #fff;
  border-radius: 12px;
  cursor: pointer;
  transition: all 0.3s ease;
}

#startBtn:hover {
  background-color: rgba(255,255,255,0.5);
  transform: scale(1.1);
}

#screenshot {
  display: none;
  max-width: 90%;
  margin-top: 20px;
  border-radius: 12px;
  opacity: 0;
  transition: opacity 2s ease-in;
}

#animatedText {
  font-size: 24px;
  margin-top: 20px;
  min-height: 60px;
  white-space: pre-wrap;
}